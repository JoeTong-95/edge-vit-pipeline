# Docker Archive

Dev-machine helper scripts archived here. Only the core files are needed for Jetson deployment.

- **Dockerfile.dev / requirements.dev.txt** — Development environment image
- **build-docker-dev-machine / .bat** — Build helper scripts for dev machines
- **run-docker-linux / run-docker-mac / run-docker-win.bat** — Cross-platform run helpers
