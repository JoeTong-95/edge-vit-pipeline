# Configuration Layer Archive

Contains deprecated and test configuration files.

## Archived Configs

- **config.cpu-test.yaml** - CPU-only test configuration (not used)
- **config.jetson.vlm-gpu-test.yaml** - GPU test config variant (not used)
- **config.jetson.vlm-gpu-lowlat.yaml** - Low-latency variant (not used)
- **config.jetson.vlm-smolvlm-256m-trt.yaml** - TensorRT vision encoder experiment (archived - TRT not viable on Jetson)

## Active Configs

See parent directory for:
- `config.yaml` - Default configuration template
- `config.jetson.vlm-smolvlm-256m.yaml` - Production configuration for Jetson deployment

## Archived Implementation Docs

- **CHANGES.md** - Detailed change history and implementation notes
